import { createSlice } from "@reduxjs/toolkit";


const engagementsSlice = createSlice(
  {
    name: "engagements",
    initialState: {},

  reducers: {},
  extraReducers: ()=> {},
}
)

export default engagementsSlice
